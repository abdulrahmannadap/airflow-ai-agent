STEP 1: Install Python 3.11
1. Go to: https://www.python.org/downloads/release/python-3119/
2. python --version

STEP 2: Install Ollama (Local AI — Optional but Recommended)
1. Go to: https://ollama.com/download/windows
2. ollama pull phi3:mini

STEP 3: Set Up the Project
1. Project setup
2. python -m venv .venv
3. Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
4. pip install -r requirements.txt
5. python -m pip install --upgrade pip setuptools wheel
6. Install pandas separately => pip install pandas==2.2.3 
                              => pip install -r requirements.txt
							   => uvicorn main:app --reload --port 8000
							    => streamlit run dashboard/app.py
								
								
STEP 4: Update the .env File
1. python -m pip install fastapi uvicorn streamlit
2. pip install fastapi uvicorn streamlit sqlalchemy pandas httpx python-dotenv loguru pdfplumber python-multipart pydantic pydantic-settings


Now Start the Dashboard (Second Window)
.venv\Scripts\Activate.ps1
streamlit run dashboard/app.py



# 1. Delete the old venv completely
Remove-Item -Recurse -Force .venv

# 2. Create fresh venv
python -m venv .venv

# 3. Activate it
.venv\Scripts\Activate.ps1

# 4. Upgrade pip first
python -m pip install --upgrade pip

# 5. Install everything
python -m pip install fastapi uvicorn[standard] streamlit httpx requests pydantic pydantic-settings sqlalchemy aiosqlite psutil apscheduler python-dotenv loguru python-dateutil aiofiles watchdog rich pandas plotly langchain langchain-community tenacity

# 6. Run backend (Terminal 1)
python -m uvicorn main:app --reload --port 8000

# 7. Run dashboard (Terminal 2)
python -m streamlit run dashboard/app.py